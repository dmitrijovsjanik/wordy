/**
 * Lives (Hearts) System Configuration
 */

/**
 * Глобальный флаг включения системы жизней.
 *
 * При false:
 *   - consumeLife и getLivesStatus возвращают «infinite» — без чтения/записи в БД
 *   - таймер восстановления не запускается
 *   - livesExhausted всегда false (попап «купить жизни» не открывается)
 *   - юзеры не наказываются за ошибки в обучении
 *
 * Отключено перед pilot: жизни наказывают за ошибки, что противоречит
 * core-механике лестницы (ошибка — обучающий момент, не провал). Код
 * сервиса/UI намеренно сохранён под флагом для возврата при разработке
 * новой монетизации.
 *
 * Парный клиентский флаг: client/src/lib/feature-flags.ts.
 */
export const LIVES_ENABLED = false;

/** Максимум жизней */
export const MAX_LIVES = 5;

/** Полное восстановление через 4 часа (в мс) */
export const LIVES_RECOVERY_MS = 4 * 60 * 60 * 1000;

/** Цена полного восстановления жизней в гемах */
export const LIVES_REFILL_GEM_COST = 250;
