/**
 * Feature flags для клиента.
 *
 * Парные серверные флаги — в `server/src/config/*-config.ts`. Меняй оба,
 * чтобы UI и API оставались согласованы.
 */

/**
 * Система жизней (сердечки).
 *
 * При false:
 *   - Lives indicator скрыт на главной
 *   - LivesExhaustedDrawer не открывается (попап «купить жизни»)
 *   - Карточка «Восстановить жизни» в магазине скрыта
 *
 * Парный серверный флаг: server/src/config/lives-config.ts → LIVES_ENABLED.
 *
 * Отключено перед pilot: жизни наказывают за ошибки, что противоречит
 * core-механике лестницы (ошибка — обучающий момент, не провал). Код
 * оставлен для возврата при разработке новой монетизации.
 */
export const LIVES_ENABLED = false;
